#include <stdio.h>

int main() {
    FILE *fptr;
    // fptr = fopen("test.txt", "w");

    // char name[100];
    // int age;
    // float cgpa;

    // printf("Enter a student's name: ");
    // fgets(name, 100, stdin);
    // printf("Enter age: ");
    // scanf("%d", &age);
    // printf("Enter cgpa: ");
    // scanf("%f", &cgpa);

    // fprintf(fptr, "%s", name);
    // fprintf(fptr, "%d\n", age);
    // fprintf(fptr, "%f\n", cgpa);

    // fclose(fptr);

    fptr = fopen("test.txt", "r");

    for ()

    return 0;
}